---
kind: checkpoint
version: 1
project: inference-recommendation-engine
task: IRE-0001-operational-ledger-relocation
status: complete
recorded_at: 2026-09-23T23:50:16Z
---

## User intent

The operational base for agents using the recommended inference models belongs
inside `inference-recommendation-engine`. It must contain model/provider usage
guidance, operational evidence, bug and debugging records, configuration
runbooks, probes/telemetry boundaries, and the evidence-aware issue ledger.

`project-continuity` is a separate checkpointing and handoff system. It must
not own the inference operational ledger. It may be used as a helper for long
running work, but it is not the implementation target for this task.

## Verified repository boundary

Target repository:

```text
inference-recommendation-engine (canonical local checkout)
remote: https://github.com/Pukujan/inference-recommendation-engine.git
HEAD: 4a4fbbb81921312356badb78d751f4f35cace731
```

Source repository that was mistakenly edited earlier:

```text
project-continuity
remote: https://github.com/biaobiao2233/project-continuity.git
HEAD: 1297914
```

The source repository no longer contains the moved ledger source files. Its
working tree is not clean: Git reports existing tracked status changes (the
content diff shows only a removed trailing blank line in
`integrations/generic-agent.md`) and three generated `__pycache__` directories
containing only `.pyc` files. Do not use a broad destructive reset. Those cache
directories were verified as generated bytecode and left in place after the
attempted cleanup was rejected by the execution policy.

## What has been moved

The following artifacts were moved into the target repository and merged by
checkpoint PR #9:

- `docs/ISSUE-LEDGER-HOST-HOOKS.md`
- `docs/ISSUE-LEDGER-INVARIANTS.md`
- `docs/ISSUE-LEDGER-OPERATIONS.md`
- `docs/ISSUE-LEDGER-RESEARCH.md`
- `docs/ISSUE-LEDGER-SPEC.md`
- `schemas/issue-ledger/v1.*.schema.json`
- `operational/scripts/*.py`
- `operational/references/issue-ledger.md`
- `operational/examples/issue-ledger-report-v1.json`
- `operational/tests/test_issue_ledger_contract.py`
- `integrations/*issue-ledger*`

They are integrated into the target repository's operational layout: scripts,
fixtures, and tests use `operational/`; adapters and examples use
`IRE_ISSUE_LEDGER_*`; schema IDs use the IRE namespace. The private ledger is
stored in the ignored `.ire/issue-ledger/ledger.sqlite3` SQLite database;
GitHub Issues own project plans and project-wide issue status, while PRs and
commit history own code changes. No local ledger database existed at resume,
so there was no private JSONL history to migrate.

Repository development uses pinned pnpm and uv lockfiles. The checkpoint
helper runs in the one canonical checkout, references its GitHub issue, and
deletes its temporary branch after a green CI-gated merge. No task worktree was
created.

## Resume sequence and verification

1. [x] Read `PROJECT.md`, this checkpoint, and `spec/INVARIANTS.md`.
2. [x] Inspect repository status and diffs in both repos; confirm the source
   repository has no remaining ledger source files.
3. [x] Adapt command paths to `operational/scripts/...` and fixture paths to
   `operational/examples/...` / `operational/tests/...`.
4. [x] Rename integration environment variables to an IRE-specific prefix and
   update the examples and tests consistently.
5. [x] Change schema `$id` values from the old continuity namespace to an
   inference-engine namespace.
6. [x] Add a documented local operational test command without putting provider
   credentials in the package.
   The public-surface scan still checks the product-name rule globally; its
   broad vocabulary rules skip only the ledger documentation/schema/script
   paths where those technical terms are part of the contract.
7. [x] Run the full verification commands:

   ```text
   pnpm test                 # 20 passed
   pnpm check:public         # passed
   pnpm test:operational     # 41 passed
   pnpm pack --dry-run       # passed; no tarball written
   ```

8. [x] Review the relocation diff and confirm `project-continuity` has no
   remaining ledger source files. Its generated bytecode cache and unrelated
   working-tree status noise remain as recorded above.
9. [x] Confirm `.ire/` is ignored and no local database/history existed to
   migrate; SQLite tests use disposable databases, including on the canonical
   checkout's filesystem.
10. [x] Pin pnpm and uv lockfiles, adapt CI and the checkpoint helper, and
    configure the hoisted pnpm linker required by the canonical filesystem.

## Checkpoint delivery evidence

- PR: https://github.com/Pukujan/inference-recommendation-engine/pull/9
- merged at: `2026-09-23T23:48:37Z`
- merge commit: `4a4fbbb81921312356badb78d751f4f35cace731`
- both required GitHub `test` checks passed before merge;
- the checkpoint helper removed the temporary branch and synchronized the
  canonical checkout to `main`; the checkout is clean and matches `origin/main`;
- the existing pre-sync safety stash remains untouched.

## Next action

Deliver the merge-evidence record update through a separate small checkpoint,
then continue the remaining work tracked by issue #8. Keep operational guidance
separate from ranking. The premium-model catalog must use only models exposed
by the configured inference gateway API; paid inference requires explicit
human authorization.

## Current non-claims

- The accepted ledger projection is still a separate read-only operational
  input and is not connected to IRE ranking behavior.
- No model/provider ranking changed in this relocation slice.
- No API key or provider request was used.
- No host adapter was installed or enabled.
- The relocation was committed, pushed as PR #9, and merged after CI passed.
