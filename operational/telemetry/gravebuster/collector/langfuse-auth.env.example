# Copy to /srv/agent-telemetry/collector/langfuse-auth.env (chmod 600). Never commit the real file.
# base64("<langfuse public key>:<langfuse secret key>") for the Langfuse project that receives traces.
LANGFUSE_OTEL_BASIC_AUTH=
